version: '3.8'
services:
  db:
    image: postgres:15
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
      POSTGRES_DB: razorpay_buildathon
    ports:
      # Host port 5433 avoids collision with an existing local PostgreSQL (5432)
      - '5433:5432'
